import React from 'react';
import FretBoard from '../misc/FretBoard';
import './index.scss';

const Home = () => {
	return (
		<div className="home">
			<FretBoard />
		</div>
	);
};

export default Home;
